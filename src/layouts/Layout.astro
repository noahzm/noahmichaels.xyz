---
import "../styles/bootstrap.scss";
import { ClientRouter } from "astro:transitions";
const {
  title = "Noah Michaels",
  description = "Graphic designer in Raleigh, NC — portfolio and selected work by Noah Michaels.",
  ...props
} = Astro.props;
const { pathname } = Astro.url;
---

<!doctype html>
<html lang="en" data-bs-theme="auto">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-title" content="Noah M" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
  <title>{title}</title>
  <meta name="description" content={description} />
    <ClientRouter />
  </head>
  <body class="container">
    <nav class="navbar navbar-expand-lg border-bottom border-body py-4">
      <div class="container-fluid">
        <a href="/" class="navbar-brand mb-0 h1 fs-3">Noah Michaels</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarToggler"
          aria-controls="navbarToggler"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarToggler">
          <div class="navbar-nav ms-auto">
            <a
              href="/"
              class:list={["nav-link", { active: pathname === "/" }]}
              aria-current={pathname === "/" ? "page" : false}>Work</a
            >
            <a
              href="/about/"
              class:list={["nav-link", { active: pathname === "/about/" }]}
              aria-current={pathname === "/about/" ? "page" : false}>About</a
            >
          </div>
        </div>
      </div>
    </nav>
    <div class="container my-4"><slot /></div>
    <script>
      // Import only what you need
      // import 'bootstrap/js/dist/alert';
      import "bootstrap/js/dist/button";
      import "bootstrap/js/dist/carousel";
      import "bootstrap/js/dist/collapse";
      // import 'bootstrap/js/dist/dropdown';
      // import 'bootstrap/js/dist/modal';
      // import 'bootstrap/js/dist/offcanvas';
      // import 'bootstrap/js/dist/popover';
      // import 'bootstrap/js/dist/scrollspy';
      // import 'bootstrap/js/dist/tab';
      // import 'bootstrap/js/dist/toast';
      // import 'bootstrap/js/dist/tooltip';
    </script>
  </body>
</html>
